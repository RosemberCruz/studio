# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure
runConfig:
  cpu: 1
  memoryMiB: 512
  maxInstances: 2
# This setting is required to ensure that the Next.js app is built before deployment.
requires:
  - build
